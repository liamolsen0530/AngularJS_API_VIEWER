import template from './omdb.html';
import controller from './omdb.controller';
import './omdb.scss';

let omdbComponent = {
  bindings: {},
  template,
  controller
};

export default omdbComponent;
